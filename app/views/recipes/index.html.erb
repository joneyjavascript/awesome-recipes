
<% content_for :title do %>
  <%= t "pages.recipes.subtitle" %>
<% end %>

<%= link_to 'Criar nova receita', new_recipe_path %>

<section class="page-block last-recipes">
    <h2>Receitas</h2>
    <ul>
    <% @recipes.each do |recipe| %>
        <li><%= link_to recipe.title, recipe %></li>
    <% end %>
    </ul>
</section>

